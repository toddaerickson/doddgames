[build]
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 10
# Mount a persistent volume at /data for the SQLite database.
# Set DATABASE_PATH=/data/doddgames.db in Railway environment variables.
